#!/bin/bash

# make sure the menu is executable
chmod +x /usr/local/bin/boot-menu.sh

# automatically start it for the user
echo "ADMIN_RELEASE=${IS_ADMIN_RELEASE} /usr/local/bin/boot-menu.sh" >> /root/.profile
